# Security: block install scripts by default.
# Prevents malicious preinstall/postinstall scripts from running
# during npm install. If a dependency genuinely needs native
# compilation, install it explicitly with:
#   npm install <pkg> --foreground-scripts
ignore-scripts=true

# Audit level: fail on high or critical vulnerabilities.
audit-level=high
